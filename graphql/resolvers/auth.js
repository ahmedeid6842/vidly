module.exports = {
    Mutation: {
        //TODO:adding authentication's Mutation resolvers
        async login(_, args, { _db }) {
            
        },
        async register(_, args, { _db }) {

        }
    }
}